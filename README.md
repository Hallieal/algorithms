# Allan's Handbook of Algorithms — v28

Changes in this archive:
- replaced the site logo and favicon assets with a muted-color version of the flowchart icon;
- kept transparent background for both the main emblem and the tab icon;
- kept the favicon tightly cropped so it appears visually larger in the browser tab;
- changed wording from `n log n` / `nlogn` to `loglinear` across the site pages where that phrase appeared;
- updated the Sorting overview to use `Loglinear Comparison Sorts`.

Updated asset files:
- `logo.png`
- `favicon.png`
- `favicon-32.png`
- `favicon-128.png`
- `apple-touch-icon.png`

A convenience source copy of the muted icon is included as:
- `logo-source-muted.png`
